apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-back.fullname" . }}
  labels:
    {{- include "blog-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "blog-back.selectorLabels" . | nindent 4 }}
